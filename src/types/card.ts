export type CreatorCardData = {
  id: number;
  name: string;
  platform: string;
  img: string;
  theme: string;
  desc: string;
};

export type ShortCardData = {
  id: number;
  title: string;
  platform: string;
  img: string;
};

export type ContentCardData = {
  id: number;
  title: string;
  platform: string;
  desc: string;
};
