import { colors, zIndex, flex, typo, radius, media, text } from "./foundations";
import { button } from "./components";

export const theme = {
  zIndex,
  flex,
  colors,
  typo,
  button,
  radius,
  media,
  text,
};
