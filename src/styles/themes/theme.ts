import { colors, zIndex, flex, typo, radius, media } from "./foundations";
import { button } from "./components";

export const theme = {
  zIndex,
  flex,
  colors,
  typo,
  button,
  radius,
  media,
};
